{% extends 'base.html.twig' %}

{% block title %}Formulaire{% endblock %}

{% block body %}
    <h1>Formulaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formulaire.id }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ formulaire.sujet }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ formulaire.firstname }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ formulaire.name }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ formulaire.message }}</td>
            </tr>
            <tr>
                <th>Numéro</th>
                <td>{{ formulaire.numero }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ formulaire.email }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_formulaireContacte_index') }}">back to list</a>

    <a href="{{ path('app_formulaireContacte_edit', {'id': formulaire.id}) }}">edit</a>

    {{ include('formulaireContacte/_delete_form.html.twig') }}
{% endblock %}
